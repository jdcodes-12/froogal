/* 
Variables & methods prepended with `_` are meant to be kept private.
They should only be used within the class.
*/
export class ReceiptCollection {
  constructor() {
    this.receiptCollection = [];
  }

  /*
    -- Getters --
    getReceiptAtIndex(index: number) : Receipt
    getCollectionSize() : int
    getReceiptCollection() : Receipt[]

    -- Setters --
    setReceiptCollection(receiptColleciton: Receipt[]) : void

    -- Utility -- 
    addReceiptToCollection(receipt: Receipt) : void
    removeReceiptFromCollection(receipt: Receipt) : void
    isEmpty() : bool
  */
};